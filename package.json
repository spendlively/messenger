{
    "name"    : "myApp",
    "version" : "0.0.1",
    "main"    : "main.js",
    "scripts" : {
        "start" : "./node_modules/.bin/electron .",
        "build": "electron-packager . myApp --all --version=1.3.4 --app-version=0.0.1 --out=dist --ignore=dist --prune --overwrite",
        "build1": "electron-packager . myApp --platform=linux --arch=x64 --version=1.3.4 --app-version=0.0.1 --out=dist --ignore=dist --prune --overwrite"
    },
    "dependencies": {
        "electron-prebuilt": "^1.3.4"
    }
}
