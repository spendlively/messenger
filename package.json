{
  "name": "Opios",
  "version": "0.0.1",
  "main": "main.js",
  "scripts": {
    "start": "./node_modules/.bin/electron .",
    "build1": "electron-packager . Opios --all --version=1.3.4 --app-version=0.0.1 --out=dist --ignore=dist --prune --overwrite",
    "build": "electron-packager . Opios --platform=linux --arch=x64 --version=1.3.4 --app-version=0.0.1 --out=dist --ignore=dist --prune --overwrite"
  },
  "dependencies": {
    "electron-prebuilt": "^1.3.4"
  },

  "description": "",
  "repository": {},
  "license": ""
}
